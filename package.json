{
  "name": "TimeEdit weekly timetable generator",
  "version": "0.1.0",
  "description": "Interactive weekly timetable built from a TimeEdit CSV export.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/papaparse": "^5.3.15",
    "typescript": "^5.6.3",
    "vite": "^6.4.3"
  },
  "dependencies": {
    "papaparse": "^5.4.1"
  }
}
